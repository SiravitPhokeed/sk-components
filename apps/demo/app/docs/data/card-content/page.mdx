import { CardContent } from "@/app/docs/components/client-components";
import DemoCard from "@/app/docs/components/DemoCard";
import LinksChipSet from "@/app/docs/components/LinksChipSet";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";
import { Actions, Button, TableCell } from "@suankularb-components/react";

export const metadata = {
  title: "Card Content",
};

## Card Content

A section of content inside Card.

<LinksChipSet material="/components/cards/overview" source="/CardContent.tsx" />

<DemoCard
  component={CardContent}
  initialProps={{
    children: (
      <>
        <p>
          Card Content can contain anything, from supporting text to Actions.
        </p>
        <Actions>
          <Button appearance="filled">Read more</Button>
        </Actions>
      </>
    ),
    className: "bg-surface-container-low max-w-80",
  }}
/>

```tsx
<CardContent>
  <p>Card Content can contain anything, from supporting text to Actions.</p>
  <Actions>
    <Button appearance="filled">Read more</Button>
  </Actions>
</CardContent>
```

### Props

- [`StyleableProps`](/docs/guides/shared-props): `className` and `style`.
- [`ElementCustomizableProps`](/docs/guides/shared-props): `element`.

<PropsTable>
  <PropsTableRow prop="children" required="always">
    <TableCell>`ReactNode`</TableCell>
    <TableCell>

      Card Content can contain anything, from supporting text to Actions.

      - Always required.

      </TableCell>

  </PropsTableRow>
</PropsTable>

### Version changes

<VersionsTable>
  <VersionsTableRow version="v3.0.0">Card Content introduced.</VersionsTableRow>
</VersionsTable>
