import { TableCell } from "@/app/docs/components/client-components";
import DemoCard from "@/app/docs/components/DemoCard";
import LinksChipSet from "@/app/docs/components/LinksChipSet";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";

export const metadata = {
  title: "Table Cell",
};

## Table Cell

A cell of a Table.

<LinksChipSet source="/TableCell.tsx" />

<DemoCard
  component={TableCell}
  initialProps={{
    children: "Cell content",
    element: "div",
    className: "w-48 border border-outline-variant",
  }}
  adjustableProps={{
    header: "boolean",
    scope: ["row", "col"],
    align: ["left", "center", "right"],
  }}
>
  `scope` only effective when `header` is `true`.
</DemoCard>

```tsx
<TableCell header scope="row" align="left">
  Cell content
</TableCell>
```

### Props

- [`StyleableProps`](/docs/guides/shared-props) available: `className` and
  `style`.
- [`ElementCustomizableProps`](/docs/guides/shared-props) available: `element`.

<PropsTable>
  <PropsTableRow prop="children" required="always">
    <TableCell>`ReactNode`</TableCell>
    <TableCell>

      The content of the cell.

      - Always required.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="header" required="never">
    <TableCell>`boolean`</TableCell>
    <TableCell>

      If the cell is a header cell, Table Cell will use `<th>` instead of `<td>`.

      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="scope" required="never">
    <TableCell>`"col" | "row" | "colgroup" | "rowgroup"`</TableCell>
    <TableCell>

      The scope of the cell, which defines the cells that the header cell
      relates to.

      - Must be `"col"`, `"row"`, `"colgroup"`, or `"rowgroup"`.
      - Only effective when `header` is `true`.
      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="align" required="never">
    <TableCell>`"left" | "center" | "right"`</TableCell>
    <TableCell>

      How the content should be positioned. It can be aligned to the left, the
      center (default), or the right.

      - Must be `"left"`, `"center"`, `"right"`.
      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="colSpan" required="never">
    <TableCell>`number`</TableCell>
    <TableCell>

      The number of columns the cell should span.

      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="rowSpan" required="never">
    <TableCell>`number`</TableCell>
    <TableCell>

      The number of rows the cell should span.

      - Optional.

      </TableCell>

  </PropsTableRow>
</PropsTable>

### Version changes

<VersionsTable>
  <VersionsTableRow version="v4.0.0">
    `colSpan` and `rowSpan` introduced. `menu` and `onMenuToggle` removed.
  </VersionsTableRow>
  <VersionsTableRow version="v3.0.0">Table Cell introduced.</VersionsTableRow>
</VersionsTable>
