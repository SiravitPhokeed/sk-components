import { Text } from "@/app/docs/components/client-components";
import DemoCard from "@/app/docs/components/DemoCard";
import LinksChipSet from "@/app/docs/components/LinksChipSet";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";
import { TableCell } from "@suankularb-components/react";

export const metadata = {
  title: "Text",
};

## Text

A piece of text with a typographic style applied.

<LinksChipSet material="/styles/typography/overview" source="/Text.tsx" />

<DemoCard
  component={Text}
  initialProps={{
    type: "headline-large",
    children: "The quick brown dog jumps over the lazy fox.",
    className: "text-center text-balance",
  }}
  adjustableProps={{
    type: [
      "display-large",
      "display-medium",
      "display-small",
      "headline-large",
      "headline-medium",
      "headline-small",
      "title-large",
      "title-medium",
      "title-small",
      "label-large",
      "label-medium",
      "label-small",
      "body-large",
      "body-medium",
      "body-small",
    ],
  }}
/>

```tsx
<Text type="headline-large">The quick brown dog jumps over the lazy fox.</Text>
```

### Props

- [`StyleableProps`](/docs/guides/shared-props): `className` and `style`.
- [`ElementCustomizableProps`](/docs/guides/shared-props): `element`.

<PropsTable>
  <PropsTableRow prop="children" required="always">
    <TableCell>`ReactNode`</TableCell>
    <TableCell>

      The text to apply the typographic styling to.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="id" required="never">
    <TableCell>`string`</TableCell>
    <TableCell>

      The unique identifier of the Text, similar to `id` on any HTML element.

      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="type" required="always">
    <TableCell>
      ```
      "display-large" | "display-medium" | "display-small"
      | "headline-large" | "headline-medium" | "headline-small"
      | "title-large" | "title-medium" | "title-small"
      | "label-large" | "label-medium" | "label-small"
      | "body-large" | "body-medium" | "body-small"
      ```
    </TableCell>
    <TableCell>

      The typographic style to apply.

      </TableCell>

  </PropsTableRow>
</PropsTable>

### Version changes

<VersionsTable>
  <VersionsTableRow version="v4.0.0">
    `button`, `caption`, and `overline` types removed. `id` introduced.
  </VersionsTableRow>
  <VersionsTableRow version="v3.2.0">Text introduced.</VersionsTableRow>
</VersionsTable>
