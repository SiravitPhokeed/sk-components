import DemoCard from "@/app/docs/components/DemoCard";
import LinksChipSet from "@/app/docs/components/LinksChipSet";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";
import { Button, MaterialIcon, TableCell } from "@suankularb-components/react";

export const metadata = {
  title: "App Drawer",
};

## App Drawer

A drawer of related apps.

<LinksChipSet source="/AppDrawer.tsx" />

<DemoCard
  component={Button}
  initialProps={{
    children: "Apps",
    appearance: "text",
    icon: <MaterialIcon icon="apps" />,
  }}
/>

```tsx
<AppDrawer>
  <AppDrawerSegment header="School apps">
    <AppDrawerItem name="Main site" logo={<img src="/main.svg" />} />
    <AppDrawerItem name="MyLearn+" logo={<img src="/my-learn.svg" />} />
  </AppDrawerSegment>
</AppDrawer>
```

### Props

- `StyleableProps` are available: `className` and `style`.

<PropsTable>
  <PropsTableRow prop="children" required="always">
    <TableCell>`ReactNode`</TableCell>
    <TableCell>

      App Drawer Segments.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="locale" required="never">
    <TableCell>`"en-US" | "th"`</TableCell>
    <TableCell>

      Allows for translation of the accessibility labels.

      - Must be `th` or `en-US`, as SKCom currently only support those 2
        languages.
      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="onOpen" required="never">
    <TableCell>`() => any`</TableCell>
    <TableCell>

      Triggers when the toggle Button opens the drawer.

      </TableCell>

  </PropsTableRow>
</PropsTable>

### Version changes

<VersionsTable>
  <VersionsTableRow version="v4.0.0">
    Design revised to better match Material Design 3. Animations revised.
  </VersionsTableRow>
  <VersionsTableRow version="v3.2.0">App Drawer introduced.</VersionsTableRow>
</VersionsTable>
