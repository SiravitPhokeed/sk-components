import { Divider } from "@/app/docs/components/client-components";
import DemoCard from "@/app/docs/components/DemoCard";
import LinksChipSet from "@/app/docs/components/LinksChipSet";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";
import { TableCell } from "@suankularb-components/react";

export const metadata = {
  title: "Divider",
};

## Divider

A Divider separates items in a list with a thin line. This is used when
separation cannot be accomplished with Cards or white space.

<LinksChipSet material="/components/divider/overview" source="/Divider.tsx" />

<DemoCard
  component={Divider}
  initialProps={{
    className: "w-full",
  }}
/>

```tsx
<Divider />
```

### Props

- `StyleableProps` are available: `className` and `style`.
- `ElementCustomizableProps` are available: `element`.

### Version changes

<VersionsTable>
  <VersionsTableRow version="v3.0.0">Divider introduced.</VersionsTableRow>
</VersionsTable>
