import LinksChipSet from "@/app/docs/components/LinksChipSet";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";
import { TableCell } from "@suankularb-components/react";

export const metadata = {
  title: "Split Layout",
};

## Split Layout

List-detail views and supporting panel layouts can be created with Split
Layout.

In a list-detail view, the left side is a list and the right is the list’s
detail. In a supporting panel layout, the main content takes focus with a
small column set aside for supporting content.

<LinksChipSet
  material="/foundations/layout/canonical-examples/overview"
  source="/SplitLayout.tsx"
/>

```tsx
<SplitLayout ratio="list-detail">
  <section>List content</section>
  <section>Detail content</section>
</SplitLayout>
```

### Props

- [`StyleableProps`](/docs/guides/shared-props) available: `className` and
  `style`.
- [`ElementCustomizableProps`](/docs/guides/shared-props) available: `element`.

<PropsTable>
  <PropsTableRow prop="children" required="always">
    <TableCell>`ReactNode`</TableCell>
    <TableCell>

      The content should have 2 sides; each element entered corresponds to a
      side.

      - Must have 2 elements.
      - Always required.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="ratio" required="always">
    <TableCell>
      ```
      "list-detail"
      | "supporting-panel"
      | [number, number]
      ```
    </TableCell>
    <TableCell>

      Choose between two presets or provide custom values. Uses the same
      12-column grid as Columns, so the two values must add up to 12.

      - `"list-detail"` — a wide list and a narrower detail pane.
      - `"supporting-panel"` — main content with a small supporting column.
      - `[number, number]` — custom column widths (e.g., `[8, 4]`).
      - Always required.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="prefer" required="never">
    <TableCell>`"left" | "right" | "both"`</TableCell>
    <TableCell>

      The side to show on mobile.

      - On mobile, there is not enough space to have 2 columns on screen at once.
        Choose which side to show on mobile, or show both with the right side
        below the left.
      - Optional.

      </TableCell>

  </PropsTableRow>
</PropsTable>

### Version changes

<VersionsTable>
  <VersionsTableRow version="v4.0.0">
    `prefer` introduced, replacing `showRightOnMobile`.
  </VersionsTableRow>
  <VersionsTableRow version="v3.0.0">
    Renamed from List Layout to Split Layout.
  </VersionsTableRow>
  <VersionsTableRow version="v1.0.0">
    List Layout introduced (predecessor to Split Layout).
  </VersionsTableRow>
</VersionsTable>
