import DemoCard from "@/app/docs/components/DemoCard";
import LinksChipSet from "@/app/docs/components/LinksChipSet";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";
import { Radio, TableCell } from "@suankularb-components/react";

export const metadata = {
  title: "Radio",
};

## Radio

A choice from a single-select set of choices. Unlike Checkbox and Switch,
Radio always appears in a group.

<LinksChipSet
  material="/components/radio-button/overview"
  source="/Radio.tsx"
/>

<DemoCard
  component={Radio}
  initialProps={{}}
  adjustableProps={{
    value: "boolean",
    disabled: "boolean",
  }}
/>

```tsx
<Radio />
```

### Usage

Radio always appears in a group. Use [Form Group](/docs/inputs/form-group) to
group related Radios and [Form Item](/docs/inputs/form-item) to label each one.

```tsx
const [atkResult, setATKResult] = useState<"negative" | "positive">();

return (
  <FormGroup label="ATK result">
    <FormItem label="Negative">
      <Radio
        value={atkResult === "negative"}
        onChange={() => setATKResult("negative")}
      />
    </FormItem>
    <FormItem label="Positive">
      <Radio
        value={atkResult === "positive"}
        onChange={() => setATKResult("positive")}
      />
    </FormItem>
  </FormGroup>
);
```

### Props

- [`ElementCustomizableProps`](/docs/guides/shared-props) available: `element`.

<PropsTable>
  <PropsTableRow prop="value" required="never">
    <TableCell>`boolean`</TableCell>
    <TableCell>

      The state of the Radio. This is useful if you want a controlled input.

      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="onChange" required="never">
    <TableCell>`(value: boolean) => any`</TableCell>
    <TableCell>

      Called when the user toggles the Radio. The state is passed in via the
      function as a boolean.

      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="disabled" required="never">
    <TableCell>`boolean`</TableCell>
    <TableCell>

      Turns the Radio gray and blocks any action associated with it.

      - [Learn how to make disabled elements less frustrating.](/docs/guides/disabling-elements)
      - Optional.

      </TableCell>

  </PropsTableRow>
</PropsTable>

### Version changes

<VersionsTable>
  <VersionsTableRow version="v3.0.0">
    React implementation introduced.
  </VersionsTableRow>
  <VersionsTableRow version="v2.6.0">Radio introduced.</VersionsTableRow>
</VersionsTable>
