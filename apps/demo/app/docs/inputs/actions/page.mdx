import { Actions } from "@/app/docs/components/client-components";
import DemoCard from "@/app/docs/components/DemoCard";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";
import { Button, TableCell } from "@suankularb-components/react";
import LinksChipSet from "@/app/docs/components/LinksChipSet";

export const metadata = {
  title: "Actions",
};

## Actions

A row of Buttons. Actions handles spacing and overflow.

<LinksChipSet source="/Actions.tsx" />

<DemoCard
  component={Actions}
  initialProps={{
    align: "center",
    children: [
      <Button key="cancel" appearance="outlined">
        Cancel
      </Button>,
      <Button key="ok" appearance="filled">
        Confirm
      </Button>,
    ],
    className: "w-full",
  }}
  adjustableProps={{
    align: ["left", "center", "right", "full"],
  }}
/>

```tsx
<Actions>
  <Button appearance="outlined">Cancel</Button>
  <Button appearance="filled">Confirm</Button>
</Actions>
```

### Props

- [`ElementCustomizableProps`](/docs/guides/shared-props) available: `element`.

<PropsTable>
  <PropsTableRow prop="children" required="always">
    <TableCell>`ReactNode`</TableCell>
    <TableCell>

      The Buttons inside Actions. You can include as many as needed, but the
      recommended limit is 3.

      - Always required.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="align" required="never">
    <TableCell>
      ```
      "left"
      | "center"
      | "right"
      | "full"
      ```
    </TableCell>
    <TableCell>

      How the Buttons should be positioned. It can be aligned to the left, the
      center, the right (default), or fill the entire width.

      - Must be `"left"`, `"center"`, `"right"`, `"full"`.
      - Optional.

      </TableCell>

  </PropsTableRow>
</PropsTable>

### Version changes

<VersionsTable>
  <VersionsTableRow version="v3.2.0">
    No longer uses `!important` overrides.
  </VersionsTableRow>
  <VersionsTableRow version="v3.0.0">
    Rewritten in the v3 monorepo.
  </VersionsTableRow>
  <VersionsTableRow version="v2.8.0">
    React implementation introduced.
  </VersionsTableRow>
  <VersionsTableRow version="v2.7.1">Actions introduced.</VersionsTableRow>
</VersionsTable>
