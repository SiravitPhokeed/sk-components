import DemoCard from "@/app/docs/components/DemoCard";
import LinksChipSet from "@/app/docs/components/LinksChipSet";
import PropsTable from "@/app/docs/components/PropsTable";
import PropsTableRow from "@/app/docs/components/PropsTableRow";
import VersionsTable from "@/app/docs/components/VersionsTable";
import VersionsTableRow from "@/app/docs/components/VersionsTableRow";
import { MaterialIcon, Switch, TableCell } from "@suankularb-components/react";

export const metadata = {
  title: "Switch",
};

## Switch

A Switch toggles something on and off. It is usually in a Form Item. Unlike
Checkbox and Radio, a Switch is independent.

<LinksChipSet material="/components/switch/overview" source="/Switch.tsx" />

<DemoCard
  component={Switch}
  initialProps={{}}
  adjustableProps={{
    value: "boolean",
    disabled: "boolean",
  }}
/>

```tsx
<Switch />
```

### Usage

Use [Form Item](/docs/inputs/form-item) to add a label to a Switch.

```tsx
const [allow, setAllow] = useState(false);

return (
  <FormItem label="Allow notifications">
    <Switch value={allow} onChange={setAllow} />
  </FormItem>
);
```

### Props

- [`ElementCustomizableProps`](/docs/guides/shared-props) available: `element`.

<PropsTable>
  <PropsTableRow prop="name" required="never">
    <TableCell>`string`</TableCell>
    <TableCell>

      The name of the Switch, used for form submission. If not provided, falls
      back to the parent Form Item’s `name`.

      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="value" required="never">
    <TableCell>`boolean`</TableCell>
    <TableCell>

      The state of the Switch. This is useful if you want a controlled input.

      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="onChange" required="never">
    <TableCell>`(value: boolean) => any`</TableCell>
    <TableCell>

      Called when the user toggles the Switch. The state is passed in via the
      function as a boolean.

      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="offIcon" required="never">
    <TableCell>`ReactElement`</TableCell>
    <TableCell>

      An icon inside the thumb when the Switch is off.

      - You are encouraged to use [Material Icon](/docs/data/material-icon) as
        the value for `offIcon`.
      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="onIcon" required="never">
    <TableCell>`ReactElement`</TableCell>
    <TableCell>

      An icon inside the thumb when the Switch is on.

      - You are encouraged to use [Material Icon](/docs/data/material-icon) as
        the value for `onIcon`.
      - Optional.

      </TableCell>

  </PropsTableRow>

  <PropsTableRow prop="disabled" required="never">
    <TableCell>`boolean`</TableCell>
    <TableCell>

      Turns the Switch gray and blocks any action associated with it.

      - Optional.

      </TableCell>

  </PropsTableRow>
</PropsTable>

### Version changes

<VersionsTable>
  <VersionsTableRow version="v4.0.0">
    `name` introduced for form submission. Switch now uses the switch role and
    announces its state as on/off.
  </VersionsTableRow>
  <VersionsTableRow version="v3.1.0">Form Item support added.</VersionsTableRow>
  <VersionsTableRow version="v3.0.0">Switch introduced.</VersionsTableRow>
</VersionsTable>
