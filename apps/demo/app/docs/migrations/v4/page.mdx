import DisclosureCard from "@/app/docs/components/DisclosureCard";
import PromptCard from "@/app/docs/components/PromptCard";
import SummaryTable from "@/app/docs/migrations/components/SummaryTable";
import SummaryTableRow from "@/app/docs/migrations/components/SummaryTableRow";
import SummaryTableSection from "@/app/docs/migrations/components/SummaryTableSection";
import prompt from "@/app/docs/migrations/v4/prompt.md";
import { Text } from "@suankularb-components/react";

export const metadata = {
  title: "Migrating to v4",
};

## Migrating to v4

v4 was released on \[TBD\]. It removed manual state management for many
components, and added support for Next.js App Router. See the
[release notes](https://github.com/suankularb-wittayalai-school/sk-components/pull/134)
for more information.

```bash
npm install @suankularb-components/react@latest
```

```bash
pnpm add @suankularb-components/react@latest
```

**This is a new major version, and that means breaking changes.** This guide
will help you migrate your codebase to v4 and also introduce new features that
you can take advantage of.

<PromptCard title="AI-assisted migration" prompt={prompt}>
  Save time by copying a structured prompt for your AI coding agent to help
  migrate your codebase to v4.
</PromptCard>

### Highlights

> 1. **App Router support** — Client Components are now marked with
>    `"use client"`.
> 2. **Setup changes** — [Theme Provider](/docs/layouts/theme-provider) is no
>    longer a wrapper, and you no longer manage the state of
>    [Nav Drawer](/docs/layout/nav-drawer) or [Snackbar](/docs/overlays/snackbar).
>    Try the new `snackbar.push()` and `snackbar.promise()` methods.
> 3. **Declarative modals and popovers** — Replace `useState` with the
>    [Invoker Commands API](https://developer.mozilla.org/en-US/docs/Web/API/Invoker_Commands_API)
>    for [Menu](/docs/overlays/menu), [Dialog](/docs/overlays/dialog), and
>    [Full-screen Dialog](/docs/overlays/fullscreen-dialog). The new
>    [Anchor](/docs/overlays/anchor) component simplifies Menu positioning.
> 4. **FAB in Root Layout** — [FAB](/docs/inputs/fab) moves from a Nav Bar prop
>    to a direct child of [Root Layout](/docs/layout/root-layout).
> 5. **`alt` and `*Attr` removed** — `alt` is removed in favor of auto-generated
>    IDs, and `*Attr` pass-through props have been removed from most components.
> 6. **Motion support removed** — You can still use
>    [Motion](https://motion.dev/docs/react) in your app, but you may need to
>    reimplement some features.
>
> A complete component-by-component checklist is available in the summary table at
> the end of this page.

#### App Router support

> You can skip this section if you are using the Pages Router.

**Client Components are now marked** with the `"use client"` directive, which
allows SKCom to be used in Next.js projects that use the App Router.

This means all components are now either Server Components or Client Components,
so **be mindful of passing props that are not
[serializable](https://react.dev/reference/rsc/use-server#serializable-parameters-and-return-values)**
to Server Components.

Sometimes you _do_ need to pass a non-serializable prop to a Client Component,
such as when passing in [`next/link`](https://nextjs.org/docs/app/api-reference/components/link) to a Server Component like
[Card](/docs/data/card). In this case, you need to **mark the parent as a Client
Component.**

```tsx
"use client";

import { Card } from "@suankularb-components/react";
import Link from "next/link";

const ExampleCard = () => (
  <Card title="Example Card" href="/example" element={Link} />
);
```

#### Setup changes

**[Theme Provider](/docs/layouts/theme-provider) is no longer a wrapper,** and can
be added anywhere in the component tree.

```tsx
import { ThemeProvider } from "@suankularb-components/react";

const App = () => (
  <>
    <ThemeProvider />
    <RootLayout>
      <PageHeader>Getting started</PageHeader>
    </RootLayout>
  </>
);
```

---

**You no longer manage the state of [Nav Drawer](/docs/layout/nav-drawer).**
Just include Page Header, Nav Bar, and Nav Drawer as direct children of
[Root Layout](/docs/layout/root-layout), and SKCom will handle the rest.

If you had previously used `useState` or React Context to manage the state of
Nav Drawer, you can remove that code.

```tsx
<RootLayout>
  <NavBar>{/* ... */}</NavBar>
  <NavDrawer>{/* ... */}</NavDrawer>
  <PageHeader>Getting started</PageHeader>
</RootLayout>
```

If you need to programmatically open or close the Nav Drawer, use the Invoker
Commands API with the `id` of `nav-drawer`.

```tsx
<Button command="show-modal" commandfor="nav-drawer">
  Open Nav Drawer
</Button>
```

---

**You no longer manage the state of [Snackbar](/docs/overlays/snackbar).** Use
the new `snackbar.push()` and `snackbar.promise()` methods.

If you had previously used `useState` or React Context to manage the state of
Snackbar, you can remove that code. If you were using the Snackbar component
itself, you can remove that as well.

```tsx
import { snackbar } from "@suankularb-components/react/helpers";

snackbar.push(
  "Deleted 3 items",
  <Button appearance="text">Undo</Button>,
  { autoDismissDurationMs: 6000 },
});
```

---

**CSS variables was renamed** to match Tailwind CSS v4’s theme variables.
If you were using the CSS variables directly, you need to update your code to
use the new names. See the [theming guide](/docs/guides/theming) for the
complete list of CSS variables.

- `--font-*` → `--font-weight-*` (font weight only, font family still uses `--font-*`)
- `--rounded-*` → `--radius-*`
- `--motion-*` → `--duration-*`
- `--easing-*` → `--ease-*`
- All color variables are now prefixed with `--color-` (e.g. `--primary` → `--color-primary`)

<DisclosureCard summary="Changes for Tailwind CSS users">

Update your Tailwind CSS configuration file to use the new variable names.

Tailwind CSS v4 users can now override SKCom styles without using `!important`.
Add this line **before** the `@import` directive in your Tailwind CSS
configuration file.

```css
@layer theme, base, skc, components, utilities;
@import "tailwindcss";
```

</DisclosureCard>

#### Declarative modals and popovers

**Replace `useState` with the Invoker Commands API** to open and close modals.

- Previously, you would have to manage the state of modals like
  [Menu](/docs/overlays/menu), [Dialog](/docs/overlays/dialog), and
  [Full-screen Dialog](/docs/overlays/fullscreen-dialog) using `useState` or
  React Context.
- This approach is still available (“controlled mode”), but you can now use the
  Invoker Commands API to open and close modals declaratively.

```tsx
// OLD: Controlled mode
const [open, setOpen] = useState(false);

return (
  <>
    <Button onClick={() => setOpen(true)}>Open Dialog</Button>
    <Dialog open={open} onClose={() => setOpen(false)}>
      {/* ... */}
    </Dialog>
  </>
);
```

```tsx
// NEW: Uncontrolled mode (Invoker Commands API)
return (
  <>
    <Button command="show-modal" commandfor="example-dialog">
      Open Dialog
    </Button>
    <Dialog id="example-dialog">{/* ... */}</Dialog>
  </>
);
```

See the [modals and popovers](/docs/guides/modals) guide for more information.

---

**New [Anchor](/docs/overlays/anchor) component makes Menu easier to use.** Wrap
the trigger and Menu in an Anchor component, and the Menu will automatically
position itself relative to the trigger.

```tsx
// OLD: Controlled mode & without Anchor
const [open, setOpen] = useState(false);

return (
  <div style={{ position: "relative" }}>
    <Button onClick={() => setOpen(true)}>Open Menu</Button>
    <Menu open={open} onClose={() => setOpen(false)}>
      <MenuItem>Item 1</MenuItem>
      <MenuItem>Item 2</MenuItem>
    </Menu>
  </div>
);
```

```tsx
// NEW: Uncontrolled mode & with Anchor
return (
  <Anchor>
    <Button command="show-popover" commandfor="example-menu">
      Open Menu
    </Button>
    <Menu id="example-menu">
      <MenuItem>Item 1</MenuItem>
      <MenuItem>Item 2</MenuItem>
    </Menu>
  </Anchor>
);
```

---

<DisclosureCard summary={
  <>
    Using the right <code>command</code> for the right modal
  </>
}>

**Open:** Use `show-popover` to open [Menu](/docs/overlays/menu) and
`show-modal` to open [Dialog](/docs/overlays/dialog) and
[Full-screen Dialog](/docs/overlays/fullscreen-dialog).

The difference is that `show-popover` will position the modal relative to the
trigger, while `show-modal` will center the modal in the viewport.

---

**Close:** Use `hide-popover` to close Menu and `request-close` to close Dialog
and Full-screen Dialog. Use `close` if you need to close the modal without
animation, although this is not recommended.

Learn more in the [modals and popovers guide](/docs/guides/modals).

</DisclosureCard>

#### FAB in Root Layout

**[FAB](/docs/inputs/fab) is now a direct child of [Root Layout](/docs/layout/root-layout).**

- Previously, FAB was a prop of Nav Bar, which made it difficult to use FAB in
  an app where Nav Bar was abstracted into a Layout and hard to access.
- Now, FAB can be placed in the Root Layout, which makes it easier to use in any
  app structure.

```tsx
// OLD: FAB in Nav Bar
<NavBar fab={<FAB icon={<MaterialIcon icon="add" />} />}>{/* ... */}</NavBar>
```

```tsx
// NEW: FAB in Root Layout
<RootLayout>
  <NavBar>{/* ... */}</NavBar>
  <FAB icon={<MaterialIcon icon="add" />} />
</RootLayout>
```

#### `alt` and `*Attr` props removed

**Remove `alt`.** Previously, `alt` was required if the children of a component
was React Elements instead of text, as it was used to generate IDs for
accessibility. Now, `alt` is no longer required, and IDs are generated
automatically.

<Text type="body-medium" className="text-on-surface-variant">
  If you were using `alt` to set the `aria-label` of a component, you might want
  to make the contents itself more accessible instead.
</Text>

```tsx
// OLD: `alt` prop required if content isn’t text
<PageHeader alt="About MyLearn+">
  <span>About</span>
  <img src="/logo.svg" />
</PageHeader>
```

```tsx
// NEW: Remove `alt`
<PageHeader>
  <span>About</span>
  <img src="/logo.svg" alt="MyLearn+" />
</PageHeader>
```

<DisclosureCard
  summary={
    <>
      Components that had <code>alt</code> prop removed
    </>
  }
>

- Chip Field
- Dialog Header
- Form Group
- Form Item
- Full-screen Dialog
- List Item Content
- Nav Bar Item
- Nav Drawer Section
- Page Header
- Search
- Select
- Tab
- Tabs Container
- Text Field

</DisclosureCard>

> While `alt` was removed from most components, **Material Icon just gained an
> `alt` prop if you need it.** Most times, though, the icon should accompany
> text or be part of other components with existing accessibility labels, so you
> likely won’t need to use it.

---

**Remove `*Attr` props.** Except for `inputAttr` in Text Field (which was not
removed), this feature never saw much use, so it was removed.

<DisclosureCard
  summary={
    <>
      Components that had <code>*Attr</code> props removed
    </>
  }
>

- `aAttr`, `buttonAttr` — Card
- `backAttr` — Page Header
- `buttonAttr` — Switch
- `hAttr` — Header
- `inputAttr` — Checkbox, Radio
- `labelAttr` — Form Item
- `legendAttr` — Form Group
- `menuAttr` — Select
- `sectionAttr` — Section
- `tdAttr` — Table Cell

</DisclosureCard>

#### Motion support removed

You can still use Motion (formerly Framer Motion) in your app, but SKCom no
longer directly supports it.

**The `useAnimationConfig()`, `useRipple()`, and `useBreakpoint()` hooks, the
`DURATION` and `EASING` constants, and the `transition` helper were removed.**

- Use [Interactive](/docs/data/interactive) instead of `useRipple()`.
- Other features can be replaced with your own implementations.

<DisclosureCard
  summary={
    <>
      Replacements for removed Motion features
    </>
  }
>

##### `useAnimationConfig()`, `DURATION`, and `EASING`

```tsx
import type { BezierDefinition } from "motion";

/** A set of durations for animations. */
export const DURATION = {
  short1: 0.05,
  short2: 0.1,
  short3: 0.15,
  short4: 0.2,
  medium1: 0.25,
  medium2: 0.3,
  medium3: 0.35,
  medium4: 0.4,
  long1: 0.45,
  long2: 0.5,
  long3: 0.55,
  long4: 0.6,
  extraLong1: 0.7,
  extraLong2: 0.8,
  extraLong3: 0.9,
  extraLong4: 1,
};

/** A set of easing definitions for animations. */
export const EASING = {
  linear: [0, 0, 1, 1] as BezierDefinition,
  standard: [0.2, 0, 0, 1] as BezierDefinition,
  standardAccelerate: [0.3, 0, 1, 1] as BezierDefinition,
  standardDecelerate: [0, 0, 0, 1] as BezierDefinition,
  emphasized: [0.2, 0, 0, 1] as BezierDefinition,
  emphasizedAccelerate: [0.3, 0, 0.8, 0.15] as BezierDefinition,
  emphasizedDecelerate: [0.05, 0.7, 0.1, 1] as BezierDefinition,
};
```

##### `transition` helper

```tsx
import type { Tween } from "motion";

/**
 * Create a Motion Tween object from a duration and an easing.
 *
 * @see {@link https://motion.dev/docs/react-transitions#tween Motion documentation}
 *
 * @param duration How long the transition takes.
 * @param easing The easing definition.
 *
 * @returns A Motion Tween object.
 */
export function transition(
  duration: Tween["duration"],
  easing: Tween["ease"],
): Tween {
  return { type: "tween", duration, ease: easing };
}
```

##### `useBreakpoint()`

Below are the breakpoints used in SKCom v4. You can use these to implement your
own `useBreakpoint()` hook if you need it.

```ts
const breakpoints = {
  sm: 600,
  md: 905,
  lg: 1440,
};
```

</DisclosureCard>

---

**`layout` and `layoutID` props were removed** from most components that had it.
Wrap the components in a
[motion component](https://motion.dev/docs/react-motion-component) instead.

### All API changes

A useful checklist for migrating your app to v4 is the following table of all
API changes. Go through each component, one-by-one, and check if you need to
update your code.

<SummaryTable>

<SummaryTableSection name="App Drawer Item">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Assist Chip">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Button">
  <SummaryTableRow name="autoFocus" type="new">
    `boolean` — Auto-focus on page load
  </SummaryTableRow>
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Card">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="aAttr" type="removed">
    Pass-through attributes for the `<a>` variant — removed
  </SummaryTableRow>
  <SummaryTableRow name="buttonAttr" type="removed">
    Pass-through attributes for the `<button>` variant — removed
  </SummaryTableRow>
  <SummaryTableRow name="layoutID" type="removed">
    Framer Motion layout animation ID — removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Checkbox">
  <SummaryTableRow name="inputAttr" type="removed">
    Raw `<input>` attribute passthrough removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Chip Field">
  <SummaryTableRow name="onNewEntries" type="new">
    `(values: string[]) => any` — receives all values when pasting or pressing
    a separator key. Replaces `onNewEntry`.
  </SummaryTableRow>
  <SummaryTableRow name="onNewEntry" type="removed">
    Replaced by `onNewEntries`, which accepts `string[]`.
  </SummaryTableRow>
  <SummaryTableRow name="required" type="new">
    `boolean` — marks the field as required
  </SummaryTableRow>
  <SummaryTableRow name="children" type="changed">
    Type narrowed: `ReactNode` → `ReactElement<typeof ChipSet>`
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Data Table">
  <SummaryTableRow name="layout" type="removed">
    Framer Motion `LayoutProps` removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Data Table Body">
  <SummaryTableRow name="align" type="new">
    `"left" | "center" | "right"` — text alignment for body cells
  </SummaryTableRow>
  <SummaryTableRow name="rowActions" type="changed">
    Type narrowed: `JSX.Element | ((row) => JSX.Element)` →
    `ReactElement<typeof SegmentedButton> |
    ((row) => ReactElement<typeof SegmentedButton>)`
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Data Table Head">
  <SummaryTableRow name="align" type="new">
    `"left" | "center" | "right"` — text alignment for header cells
  </SummaryTableRow>
  <SummaryTableRow name="colSpans" type="new">
    `number[]` — column span values per header cell
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Dialog">
  <SummaryTableRow name="id" type="new">
    `string` — for Invoker Commands (`command="show-modal" commandfor={id}`).
    Auto-generated if omitted.
  </SummaryTableRow>
  <SummaryTableRow name="open" type="changed">
    Now **optional**. Omit for uncontrolled mode where the browser manages
    open/close natively via Invoker Commands.
  </SummaryTableRow>
  <SummaryTableRow name="onClose" type="changed">
    Now **optional**. Only needed in controlled mode.
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Dialog Header">
  <SummaryTableRow name="alt" type="removed">
    Accessibility description — removed (IDs use `useId()`)
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Filter Chip">
  <SummaryTableRow name="href" type="new">
    `string` — navigation URL (previously unavailable)
  </SummaryTableRow>
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="menu" type="changed">
    Fragment with Menu Items instead of Menu
  </SummaryTableRow>
  <SummaryTableRow name="onMenuToggle" type="removed">
    Menu toggle callback removed
</SummaryTableRow>

</SummaryTableSection>

<SummaryTableSection name="Floating Action Button">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="stateOnScroll" type="removed">
    Scroll-based state toggling removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Form Group">
  <SummaryTableRow name="legendElement" type="new">
    `ElementType` — custom element for the legend
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
  <SummaryTableRow name="legendAttr" type="removed">
    Legend element passthrough — removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Form Item">
  <SummaryTableRow name="children" type="changed">
  Type narrowed to `ReactElement<typeof Checkbox | typeof Radio | typeof
      Switch>`
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
  <SummaryTableRow name="labelAttr" type="removed">
    Label element passthrough — removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Full-screen Dialog">
  <SummaryTableRow name="id" type="new">
    `string` — for Invoker Commands. Auto-generated if omitted.
  </SummaryTableRow>
  <SummaryTableRow name="locale" type="new">
    `"en-US" | "th"` — translation of accessibility labels
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Header">
  <SummaryTableRow name="hAttr" type="removed">
    Heading element passthrough — removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Input Chip">
  <SummaryTableRow name="tooltip" type="new">
    `string` — hover tooltip text
  </SummaryTableRow>
  <SummaryTableRow name="locale" type="new">
    `"en-US" | "th"` — translation of accessibility labels
  </SummaryTableRow>
  <SummaryTableRow name="deleteCommand" type="new">
    Invoker Commands `command` for the delete button
  </SummaryTableRow>
  <SummaryTableRow name="deleteCommandfor" type="new">
    Invoker Commands `commandfor` for the delete button
  </SummaryTableRow>
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="editable" type="removed">
    Edit mode removed entirely
  </SummaryTableRow>
  <SummaryTableRow name="value" type="removed">
    Only meaningful with `editable` — removed
  </SummaryTableRow>
  <SummaryTableRow name="onChange" type="removed">
    Only meaningful with `editable` — removed
  </SummaryTableRow>
  <SummaryTableRow name="onEditExit" type="removed">
    Only meaningful with `editable` — removed
  </SummaryTableRow>
  <SummaryTableRow name="layoutID" type="removed">
    Framer Motion layout ID — removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Interactive">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="attr" type="removed">
    Generic pass-through `<ElementProps>` replaced by standard `...props`
    spread to underlying `<button>` / `<a>`
  </SummaryTableRow>
  <SummaryTableRow name="ElementProps" type="removed">
    Component is no longer generic — no `<Interactive<HTMLAnchorElement>>`
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="List Item">
  <SummaryTableRow name="containerElement" type="new">
    `ElementType` — custom element for the container
  </SummaryTableRow>
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="List Item Content">
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Material Icon">
  <SummaryTableRow name="alt" type="new">
    `string` — accessibility label for the icon
  </SummaryTableRow>
  <SummaryTableRow name="directional" type="new">
    `boolean` — flips the icon horizontally in RTL
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Menu">
  <SummaryTableRow name="id" type="new">
    `string` — for Invoker Commands. Auto-generated if omitted.
  </SummaryTableRow>
  <SummaryTableRow name="anchor" type="new">
    `string` — CSS Anchor Positioning name (dashed-ident). Auto-resolved from
    `<Anchor>` context when wrapped.
  </SummaryTableRow>
  <SummaryTableRow name="onClose" type="new">
    `() => void` — called when the popover closes
  </SummaryTableRow>
  <SummaryTableRow name="onBlur" type="removed">
    Replaced by `onClose`
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Menu Item">
  <SummaryTableRow name="dangerous" type="new">
    `boolean` — renders with error color
  </SummaryTableRow>
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="value" type="changed">
    Type narrowed: `any` → `string`
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Nav Bar">
  <SummaryTableRow name="onNavToggle" type="changed">
    Was required (`() => any`), now **optional** (`() => void`)
  </SummaryTableRow>
  <SummaryTableRow name="end" type="changed">
    Type widened: `JSX.Element` → `ReactNode`
  </SummaryTableRow>
  <SummaryTableRow name="brand" type="removed">
    Brand logo slot removed
  </SummaryTableRow>
  <SummaryTableRow name="fab" type="removed">
    Floating action button slot removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Nav Bar Item">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
  <SummaryTableRow name="badge" type="removed">
    Badge indicator removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Nav Drawer">
  <SummaryTableRow name="open" type="removed">
    No longer consumer-controlled. Use `command="show-modal"
    commandfor="nav-drawer"`.
  </SummaryTableRow>
  <SummaryTableRow name="onClose" type="removed">
    No longer needed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Nav Drawer Item">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Nav Drawer Section">
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Page Header">
  <SummaryTableRow name="appDrawer" type="changed">
    Type narrowed: `JSX.Element` → `ReactElement<typeof AppDrawer>`
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Deprecated — removed
  </SummaryTableRow>
  <SummaryTableRow name="backAttr" type="removed">
    Back button passthrough attributes — removed
  </SummaryTableRow>
  <SummaryTableRow name="brand" type="removed">
    Deprecated — removed
  </SummaryTableRow>
  <SummaryTableRow name="homeURL" type="removed">
    Deprecated — removed
  </SummaryTableRow>
  <SummaryTableRow name="icon" type="removed">
    Deprecated — removed
  </SummaryTableRow>
  <SummaryTableRow name="title" type="removed">
    Deprecated — removed; use `children` instead
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Radio">
  <SummaryTableRow name="inputAttr" type="removed">
    Raw `<input>` attribute passthrough removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Search">
  <SummaryTableRow name="hotkey" type="new">
    `boolean | string` — keyboard shortcut to focus the field (e.g. `"/"` or
    `true` for `/`)
  </SummaryTableRow>
  <SummaryTableRow name="onSearch" type="changed">
    Now passes the search query — `(value: string) => any`
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
  <SummaryTableRow name="children" type="removed">
    Suggestion list/dropdown no longer supported — remove any `<MenuItem>`
    children
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Section">
  <SummaryTableRow name="sectionAttr" type="removed">
    Section element passthrough — removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Select">
  <SummaryTableRow name="value" type="changed">
    Type narrowed: `any` → `string` (generic `Value extends string`)
  </SummaryTableRow>
  <SummaryTableRow name="onChange" type="changed">
    Now typed as `(value: Value) => any` with generic `Value`
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
  <SummaryTableRow name="menuAttr" type="removed">
    Menu is now fully managed internally — no passthrough needed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Snackbar">
  <SummaryTableRow name="id" type="new">
    `string` — for imperative API access. Auto-generated if omitted.
  </SummaryTableRow>
  <SummaryTableRow name="persistent" type="new">
    `boolean` — prevent auto-dismiss
  </SummaryTableRow>
  <SummaryTableRow name="autoDismissDurationMs" type="new">
    `number` — time until auto-dismiss
  </SummaryTableRow>
  <SummaryTableRow name="action" type="changed">
    Type narrowed to `ReactElement<typeof Button>`
  </SummaryTableRow>
  <SummaryTableRow name="open" type="removed">
    No longer consumer-controlled
  </SummaryTableRow>
  <SummaryTableRow name="onClose" type="removed">
    No longer needed
  </SummaryTableRow>
  <SummaryTableRow name="onExitComplete" type="removed">
    No longer needed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Split Layout">
  <SummaryTableRow name="prefer" type="new">
    `"left" | "right" | "both"` — which pane to show on small screens
  </SummaryTableRow>
  <SummaryTableRow name="showRightOnMobile" type="removed">
    Replaced by `prefer`
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Suggestion Chip">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Switch">
  <SummaryTableRow name="buttonAttr" type="removed">
    Raw `<button>` attribute passthrough removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Tab">
  <SummaryTableRow name="command" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="commandfor" type="new">
    Invoker Commands API support
  </SummaryTableRow>
  <SummaryTableRow name="containerID" type="removed">
    Parent container ID — no longer needed (uses React Context)
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Table Cell">
  <SummaryTableRow name="colSpan" type="new">
    `number` — HTML colspan
  </SummaryTableRow>
  <SummaryTableRow name="rowSpan" type="new">
    `number` — HTML rowspan
  </SummaryTableRow>
  <SummaryTableRow name="menu" type="removed">
    Inline menu removed
  </SummaryTableRow>
  <SummaryTableRow name="onMenuToggle" type="removed">
    Menu toggle callback removed
  </SummaryTableRow>
  <SummaryTableRow name="tdAttr" type="removed">
    Raw `<td>` attribute passthrough removed
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Table Row">
  <SummaryTableRow name="actions" type="changed">
    Type narrowed: `JSX.Element` → `ReactElement<typeof SegmentedButton>`
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Tabs Container">
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Text">
  <SummaryTableRow name="id" type="new">
    `string` — HTML `id` attribute
  </SummaryTableRow>
  <SummaryTableRow name="type" type="changed">
    Removed `"button"`, `"caption"`, `"overline"`.
  </SummaryTableRow>
</SummaryTableSection>

<SummaryTableSection name="Text Field">
  <SummaryTableRow name="type" type="new">
    Native input types: `"color"`, `"date"`, `"datetime-local"`, `"email"`,
    `"file"`, `"month"`, `"number"`, `"password"`, `"search"`, `"tel"`,
    `"text"`, `"time"`, `"url"`, `"week"`. Default `"text"`.
  </SummaryTableRow>
  <SummaryTableRow name="alt" type="removed">
    Removed (IDs use `useId()`)
  </SummaryTableRow>
  <SummaryTableRow name="canClear" type="removed">
    Clear button removed
  </SummaryTableRow>
</SummaryTableSection>

</SummaryTable>

<DisclosureCard summary="Components with no API changes">

- Actions
- App Drawer
- App Drawer Segment
- Avatar
- Card Content
- Card Header
- Chip Set
- Columns
- Content Layout
- Data Table Content
- Data Table Filters
- Data Table Pagination
- Data Table Search
- Dialog Content
- Divider
- List
- Progress
- Root Layout
- Segmented Button
- Table
- Table Body
- Table Foot
- Table Head
- Toggle Button

</DisclosureCard>
