{
  "private": true,
  "name": "sk-components",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.4.0"
  }
}
